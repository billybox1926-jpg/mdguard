# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

### Added
- Recursive Markdown file discovery with configurable excludes.
- Built-in rules: `empty-link`, `final-newline`, `heading-jump`, `line-length`, `trailing-whitespace`.
- Optional rules: `duplicate-headings`, `missing-h1`.
- JSON and GitHub-format output.
- Conservative autofix (trailing whitespace, final newline).
- Baseline support for suppressing known findings.
- Inline comment suppressions.
- Project configuration via CLI flags (`--max-length`, `--strict`, `--enable`, `--disable`, `--exclude`, `--rules`).
- Local test suite and validation script.
- Release checklist at `docs/RELEASE_CHECKLIST.md`.

### Changed
- N/A

### Fixed
- N/A
